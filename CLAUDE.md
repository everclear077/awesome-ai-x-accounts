# Claude project context

Read and follow [AGENTS.md](AGENTS.md) before working.
Read [the AI workflow](docs/ai-workflow.md) for task prompts and acceptance checks.
Copy bios verbatim and record exact follower snapshots; do not write recommendations.
Canonical data lives in `data/accounts.json`; rebuild generated files with
`python scripts/catalog.py build`. Keep tool-specific configuration thin.
